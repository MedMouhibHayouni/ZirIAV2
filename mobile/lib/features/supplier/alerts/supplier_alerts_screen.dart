import 'package:flutter/material.dart';

class SupplierAlertsScreen extends StatelessWidget {
  const SupplierAlertsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Alertes Marché')),
      body: const Center(child: Text('Alerts under construction')),
    );
  }
}
