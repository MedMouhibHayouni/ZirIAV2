export enum InstitutionType {
  APIA = 'APIA',
  CRDA = 'CRDA',
}

export enum InstitutionLevel {
  REGIONAL = 'REGIONAL',
  NATIONAL = 'NATIONAL',
}

export enum OfficeRole {
  DIRECTOR = 'DIRECTOR',
  AGENT = 'AGENT',
  VIEWER = 'VIEWER',
}
